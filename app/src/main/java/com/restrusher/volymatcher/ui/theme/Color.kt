package com.restrusher.volymatcher.ui.theme

import androidx.compose.ui.graphics.Color

// VolyMatcher design tokens — warm cream + sport orange
// Replace these values when importing the final design-system color file.

// Light mode palette
val Cream = Color(0xFFF4F1EA)         // page background
val Paper = Color(0xFFFAF7F0)         // card / elevated surface
val Ink = Color(0xFF1A1613)           // primary text / dark surface
val InkSoft = Color(0xFF5A534C)       // secondary text
val InkFaint = Color(0xFFA8A096)      // tertiary / subtle text
val Line = Color(0xFFE4DDD0)          // dividers / borders
val Accent = Color(0xFFFF5C2B)        // sport orange — primary action
val AccentDeep = Color(0xFFE04511)    // pressed / deep accent

// Secondary palette
val Lime = Color(0xFFC7E84A)          // secondary accent — neon lime
val Sky = Color(0xFF9FC6E8)           // tertiary — cool blue
val Clay = Color(0xFFC86A4A)          // warm tertiary — clay

// Dark mode palette
val DarkBackground = Color(0xFF0F0D0B)  // deep warm black
val DarkSurface = Color(0xFF1C1815)     // elevated charcoal
val DarkOnSurface = Color(0xFFF4F1EA)   // off-white text
val DarkOnSurfaceVariant = Color(0xFFA8A096)
val DarkOutline = Color(0xFF5A534C)
val DarkOutlineVariant = Color(0xFF2A2521)
val DarkHeroBg = Color(0xFF000000)      // hero card bg in dark mode
